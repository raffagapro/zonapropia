@props([
  'subtitle' => $subtitle,
  'title' => $title,
  'par' => $par,
])

<p class="carrousel-st mb-0 text-white">{{$subtitle}}</p>
<h1 class="carrousel-title text-white">{{$title}}</h1>
<p class="carrousel-text text-white">{{$par}}</p>
