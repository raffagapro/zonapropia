@props([
  'subtitle' => $subtitle,
  'title' => $title,
  'par' => $par,
])

<p class="carrousel-st-white mb-0">{{$subtitle}}</p>
<h1 class="carrousel-title-white">{{$title}}</h1>
<p class="carrousel-text-white">{{$par}}</p>
