@props([
  'subtitle' => $subtitle,
  'title' => $title,
  'par' => $par,
])

<p class="carrousel-st mb-0">{{$subtitle}}</p>
<h1 class="carrousel-title">{{$title}}</h1>
<p class="carrousel-text">{{$par}}</p>
